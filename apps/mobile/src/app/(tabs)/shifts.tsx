import { View, Text } from 'react-native';

export default function ShiftsScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-slate-50">
      <Text className="text-lg font-semibold text-slate-800">Manajemen Shift</Text>
      <Text className="text-slate-500 mt-2">Segera hadir</Text>
    </View>
  );
}
