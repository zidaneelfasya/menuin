const postgres = require('postgres'); require('dotenv').config({ path: '.env.local' }); const sql = postgres(process.env.DATABASE_URL); sql.unsafe('CREATE TABLE IF NOT EXISTS device_pairing_codes (id uuid PRIMARY KEY DEFAULT gen_random_uuid(), tenant_id uuid NOT NULL REFERENCES tenants(id), code text UNIQUE NOT NULL, status text NOT NULL DEFAULT \'PENDING\', expires_at timestamp with time zone NOT NULL, created_at timestamp with time zone NOT NULL DEFAULT now());').then(() => { console.log('success'); sql.end(); }).catch(console.error);
